# GPU 4–7：4B 全量 baseline 启动记录

本次运行在独立目录 `agent-lightning-speedtrial-20260913`，分支
`experiment/swe-speed-20260913`。GPU 0–3 上的既有训练和源代码目录不作修改。

## 配置

| 项目 | 本次值 |
|---|---|
| 模型 | Qwen3-4B-Instruct-2507，原始权重重新训练 |
| GPU | 物理 4,5,6,7，4 张 A800 80GB |
| 数据 | python-full-v7：6248 train / 470 val |
| 训练长度 | 4 epochs；每 epoch 196 batches；总计 784 optimizer steps，保留尾批 |
| 每步任务数 / PPO minibatch | 32 / 32 |
| actor / critic update microbatch | 2 / 2 |
| actor、reference log-prob / critic forward microbatch | 全部 2 |
| 采样并发 | 32 agents；每个 vLLM 实例 max_num_seqs=8 |
| vLLM max_num_batched_tokens | 16384 |
| 验证 | 开始训练前及每 20 步，全量 470 条；末步也验证 |
| checkpoint | 每 40 步及末步，最多保留 3 组 actor+critic；保留 optimizer/RNG |
| 任务预算 | 32 turns；单次输出 4096 tokens；上下文 65536 tokens |
| PPO | CAPO backend；gamma=lambda=1；clip=0.2；KL=0.001 |
| 学习率 | actor=1e-6，critic=1e-5；无 warmup |
| 隔离 | Lightning port 18501；独立 Ray 临时目录 |

这些加速调整扩大采样队列和推理调度容量，未缩短任务预算，未改变 PPO
目标、有效 minibatch 或更新次数。并发调度仍可能改变随机采样的具体轨迹，
不能承诺与旧运行逐样本一致。

此前完整验证的实测耗时从 97 分 34 秒降到 34 分 54 秒（约 2.80 倍）。
该结果不代表整个训练 step 同样加速；训练更新 microbatch 仍为 2，
新的完整训练 step 耗时需本次实测。

## 数据变化和评分修复

Paramiko 参数化测试 ID 的空格曾被截断；评分器现在针对已验证的两个 ID
恢复完整名称，并按完整测试 ID 解析结果。此次不修改 CAPO 更新代码。
修复后 17 条相关任务的参考补丁全部通过，14 条的空补丁失败。
其余 3 条训练任务的空补丁和参考补丁都通过所有 113 项指定测试。

用户于 2026-09-14 明确批准剔除这 3 条，避免虚假成功奖励：

- `paramiko__paramiko.23f92003.lm_rewrite__22j0m5ms`
- `paramiko__paramiko.23f92003.lm_rewrite__ekwh2bqe`
- `paramiko__paramiko.23f92003.func_basic__pi2mww3n`

新数据位于 `/media/ubuntu/D1/zsj/agent-lightning-runtime/data/swe-smith-training/python-full-v7`。
保留此前 64 条排除记录，合计排除 67 条；原始数据及 v6 不变。
v7 的 `manifest.json` 和 `exclusions.json` 记录授权、任务原始哈希、
空补丁/参考补丁结果及证据文件哈希。保留任务和整个验证文件字节不变。
可复现脚本：`prepare_paramiko_filtered.py`。

## 验收和运行状态

- 评分回归测试：`tests/examples/test_full_python.py`，25 passed。
- 启动脚本通过 `bash -n`。
- 新评分器和新数据须通过独立的 124 镜像空补丁/参考补丁检查，以及所有
  保留任务的分支检查；不复用旧评分器的签名绕过启动检查。
- 检查目录：`/media/ubuntu/D1/zsj/agent-lightning-runtime/logs/swe-full-python-envs-speed-20260914-01`。
- 当前状态：启动前检查中，尚未声明训练已启动。

## 复现入口

在独立远端目录执行：

```bash
export AGL_TRAIN_TAG=capo-swe-pythonfull-v7-4b-gpu47-20260914-01
bash examples/multiturn_ppo/run_full_python_ppo.sh
```

脚本内显式选择 GPU 4–7；开始前检查这些卡空闲、端口可用和磁盘余量。
日志将位于 `/media/ubuntu/D1/zsj/agent-lightning-runtime/logs/training-capo-swe-pythonfull-v7-4b-gpu47-20260914-01/`。
`trainer.log` 为训练/验证进度，`metrics.jsonl` 为逐步指标，
`gpu.csv` 为显存与利用率，`run.exit` 仅在作业退出后出现。
