# SWE PPO baseline 启动工作汇报

> 历史快照：这是此前 32 条训练 / 6 条验证的子集运行，后来已按授权停止。
> 不代表当前正在运行，也不代表完整 Python 数据集已启动。
> 当前状态见 [全量准备与切换记录](全量Python_baseline准备与切换_2026-09-12.md)。

日期：2026-09-12。启动验收快照：北京时间 **20:52:50**。

## 本次结果

已启动 Qwen3-4B-Instruct-2507 的全程 PPO baseline，仅使用物理 **GPU 0、1、2、3**。
从已验收的 `global_step_2` 恢复，训练目标为累计 **32 个 global step**，本次新增 30 步。
后台运行正常，完整初始验证为 **1/6**；已进入第 3 步训练。
验收快照时，该批已完成 **8/32** 个训练任务，其中 **1 个成功**，其余任务仍在执行。
这只是部分采样结果，不能写成整批成功率，也不能写成第 3 步参数更新已经完成。

本报告中的“全程/全量”指当前已缓存的 SWE-smith exceptiongroup **全部 32 train / 6 val**
及完整 32 步训练预算。它不是完整 SWE-bench，也没有新增其他仓库的数据或容器镜像。
这是已有两步 baseline 的延续，未重新从基座开始训练。

## 运行位置

| 项目 | 值 |
| --- | --- |
| 服务器 | A800，用户 ubuntu |
| 运行标识 | `training-capo-swe-full-4b-20260912-01` |
| screen | `1728692.capo-swe-full-4b-0912`，Detached |
| 本地服务 | `http://127.0.0.1:18396` |
| 服务器代码 | `/media/ubuntu/D1/zsj/agent-lightning-main/agent-lightning-main` |
| 实验目录 | `/media/ubuntu/D1/zsj/agent-lightning-runtime/logs/training-capo-swe-full-4b-20260912-01` |
| 恢复来源 | `/media/ubuntu/D1/zsj/agent-lightning-runtime/logs/training-capo-swe-editor-resume-4b-20260912-01/checkpoints/global_step_2` |
| 模型 | `/media/ubuntu/D1/zsj/GOPD/G-OPD-main/models/Qwen3-4B-Instruct-2507` |

源码的权威副本仍是 Windows：
`D:\ai project\RL学习\agent-lightning-main\agent-lightning-main`。
本次启动前核对了 44 个相关源码文件，本地和服务器 SHA256 全部一致。
本次启动没有继续修改 PPO 算法源码，也没有变更环境、模型或数据集。

## 实际生效配置

| 配置 | 值 |
| --- | --- |
| 算法 | 移植 CAPO 的 `token_gae + vanilla PPO` baseline，非论文中的完整动作级 CAPO 算法 |
| 训练布局 | 单机 4 卡 FSDP，actor / critic 使用相同的前四张卡 |
| 推理布局 | 4 个 vLLM 副本，每副本 TP=1；每副本显存比例 0.30 |
| 同时运行的 agent | 最多 8 个 |
| 训练 / 验证任务数 | 32 / 6，两个集合不混用 |
| 任务 batch / 调用 minibatch | 32 / 32 |
| 每卡 microbatch | 1 |
| 总步数 / epoch 上限 | 32 / 32；恢复后执行 step 3–32 |
| 每个任务最大工具轮次 | 32 |
| 单次最大输出 | 4096 token |
| 上下文上限 | 65536 token |
| 工具观察上限 | 32000 字符 |
| 训练采样 | temperature=1，top-p=1，top-k=-1 |
| 验证采样 | temperature=.7，top-p=.8，top-k=20 |
| actor / critic 学习率 | 1e-6 / 1e-5，constant scheduler |
| 学习率预热 / critic warmup | 0 / 0 |
| gamma / lambda | 1 / 1 |
| KL | actor KL loss，系数 .001，`low_var_kl` |
| PPO clip | low=.2，high=.2，dual clip=3 |
| actor / critic PPO epochs | 1 / 1 |
| entropy | 系数 0，不计算无用的 entropy 统计 |
| 输出层 | veRL 原生 fused kernels，Torch backend |
| 工具修复 | 精确编辑、提交检查、语法检查；新建复现脚本不进入源码补丁 |
| 验证频率 | 启动先验证；之后每 2 步，最终步也验证 |
| 保存频率 | 每 4 步，最终步也保存；首个新 checkpoint 预计在 step 4 |
| checkpoint 保留 | 本运行 actor / critic 各保留最新 1 套，含模型、优化器和额外状态；actor 含 HF 导出 |

与已成功的恢复验收配置逐字段比较，变化仅涉及训练长度、验证/保存频率、保留数量，
以及运行名称、端口、输出目录、恢复来源等元数据。模型、PPO 参数、奖励与任务预算没有额外变化。
配置差异证据：实验目录内 `baseline-config-diff.json`。

## 启动安全检查

1. **资源可用**：启动前 GPU 0–3 各仅占用 17 MiB；可用系统内存约 902.36 GiB；
   磁盘可用 298.55 GiB。启动入口要求至少 260 GiB 空闲。
2. **GPU 隔离**：沿 screen 进程树核对本次 GPU 进程，所有训练/推理进程只出现在物理 0–3
   对应的 GPU UUID 上。没有占用 4–7，也没有终止其他任务。
3. **恢复来源有效**：来源运行 exit 0，`verification.json` 已通过；actor、critic 四 rank
   的保存优化器计数均为 22，checkpoint 文件齐全。
4. **实际恢复完成**：读取四个 worker 的原始日志，actor/critic × 4 rank ×
   model/optimizer/RNG/scheduler 共 **32 项加载记录全部存在**。
5. **实际请求版本正确**：初始验证及已经完成的训练调用全部使用模型版本 2。
6. **完整验证完成**：6/6 验证任务正常返回，指标 reward=1/6；37 次调用的完整历史、
   prompt token、响应和实际评分逐项核对一致，0 观察截断。
7. **训练已经开始**：进度条恢复到 2/32 后进入第 3 步；快照时已完成 8 条训练轨迹，
   其中 1 条成功。服务健康检查 HTTP 200，未发现框架 Traceback、CUDA OOM 或 Ray actor 错误。

以上是“成功恢复并进入训练”的验收，不代表剩余 30 步已经完成，也不保证未来不会出现故障。
已有测试退化提示不影响此次启动；模型生成的错误补丁仍可能导致独立评分失败或超时，
按原终局二值奖励记录，不隐藏失败轨迹。

## 磁盘安排

一套当前 4B 的完整 actor + critic checkpoint 实测约 **107.83 GiB**。
本运行保留最新一套；框架在新保存完成后轮换旧文件，保存期间需为新旧文件并存留余量。
按两套完整 checkpoint 保守估算约 215.65 GiB，相对启动时空闲空间仍有约 82.9 GiB
用于日志、审计张量和其他临时文件；稳定保留一套后的余量更大。

本次没有再删除 checkpoint。先前按授权清理的约 425 GiB 失败 checkpoint 已有独立清单，
实验日志和轨迹均保留；之前两个有效 4B checkpoint 也保留。共享磁盘的其他写入仍会影响余量。
验收快照时剩余 **298.54 GiB**。

## 本次实际启动命令

在服务器代码目录，通过以下命令启动。运行标识和端口已经使用，不能原样重复启动第二份。

```bash
screen -dmS capo-swe-full-4b-0912 env \
  AGL_GPUS=0,1,2,3 \
  AGL_TRAIN_TAG=capo-swe-full-4b-20260912-01 \
  AGL_TRAIN_PORT=18396 \
  AGL_MIN_FREE_GIB=260 \
  bash /media/ubuntu/D1/zsj/agent-lightning-main/agent-lightning-main/examples/multiturn_ppo/run_checked_swe_ppo.sh \
  --steps 32 trainer.total_epochs=32 \
  trainer.save_freq=4 trainer.test_freq=2 \
  trainer.max_actor_ckpt_to_keep=1 trainer.max_critic_ckpt_to_keep=1 \
  trainer.val_before_train=true trainer.resume_mode=resume_path \
  trainer.resume_from_path=/media/ubuntu/D1/zsj/agent-lightning-runtime/logs/training-capo-swe-editor-resume-4b-20260912-01/checkpoints/global_step_2
```

`/media/ubuntu/D1/zsj/agent-lightning-runtime/logs/capo-swe-full-4b-20260912-01-launch.json`
保存了完整启动参数、时间和资源快照，没有保存临时认证密钥。

## 后续查看与判断

实验目录中的文件：

- `metrics.jsonl`：完成的训练步和验证指标。第 3 步完成之前只有恢复后的初始验证记录是正常的。
- `trainer.log`：模型加载、权重恢复、训练进度和异常。
- `gpu.csv`：仅记录物理 GPU 0–3 的使用情况。
- `agent/`、`traces/`、`ppo-audit/`：原始操作、评分、调用和训练张量。
- `startup-check.json`：本报告对应的启动验收快照。
- `startup-restoration-check.json`：32 项恢复记录。
- `startup-validation-integrity.json`：6 个初始验证任务的完整调用/评分核对。
- `resolved-config.json`、`provenance.json`、`datasets.json`：实际配置、源码/环境凭据和数据。
- `run.exit`：进程结束时才写入；不存在仅表示仍在运行，不能当作成功。正常完成应为 0，
  且有最终指标和完整 checkpoint，之后再运行 `verify_run.py` 验收。

可只读查看：

```bash
tail -n 3 /media/ubuntu/D1/zsj/agent-lightning-runtime/logs/training-capo-swe-full-4b-20260912-01/metrics.jsonl
tail -n 60 /media/ubuntu/D1/zsj/agent-lightning-runtime/logs/training-capo-swe-full-4b-20260912-01/trainer.log
df -h /media/ubuntu/D1
nvidia-smi -i 0,1,2,3
```

## 已知限制

前期修复已经使同一未训练 4B 从连续三次 train 0/32 恢复到两次 3/32、4/32，
真实两步 PPO 与断点恢复通过，76 项相关回归通过。详细证据见
[端到端排查报告](SWE_DIAGNOSIS_2026-09-12.md)。

此前两步训练尚未提高验证分数，当前也不能从首批部分正奖励推断学习提升。
少量采样/重算 logprob 的大差异尚未完全归因；独立检查排除了已检查轨迹的 token 错位，
但未复现最大的在线异常。此次长程 baseline 在保留这些限制的情况下启动，用来观察后续
完整训练和验证曲线；不能写成所有疑点都已消除。
